package com.minersstudios.wholib.velocity.gui;

import com.minersstudios.wholib.velocity.WhoMine;
import com.minersstudios.wholib.gui.GuiManager;
import org.jetbrains.annotations.NotNull;

public class VelocityGuiManager extends GuiManager<WhoMine> {

    public VelocityGuiManager(final @NotNull WhoMine module) {
        super(module);
    }
}
